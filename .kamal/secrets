# Secrets defined here are available for reference under registry/password, env/secret, builder/secrets,
# and accessories/*/env/secret in config/deploy.yml. All secrets should be pulled from either
# password manager, ENV, or a file. DO NOT ENTER RAW CREDENTIALS HERE! This file needs to be safe for git.

# ===== REQUIRED SECRETS FOR DEPLOYMENT =====

# 1. Rails Master Key (for encrypted credentials)
# Improve security by using a password manager. Never check config/master.key into git!
RAILS_MASTER_KEY=$(cat config/master.key)

# 2. Claude API Key (required for AI training plan generation)
# TODO: Set this environment variable before deploying
# Get your key from: https://console.anthropic.com/
# Option 1: Export in your shell
#   export CLAUDE_API_KEY="sk-ant-..."
# Option 2: Load from .env file (make sure .env is in .gitignore!)
#   CLAUDE_API_KEY=$(grep CLAUDE_API_KEY .env | cut -d '=' -f2)
CLAUDE_API_KEY=${CLAUDE_API_KEY:?CLAUDE_API_KEY environment variable must be set}

# 3. Container Registry Password
# TODO: Uncomment and configure based on your registry choice
# For Docker Hub:
#   KAMAL_REGISTRY_PASSWORD=${DOCKER_HUB_TOKEN:?DOCKER_HUB_TOKEN must be set}
# For GitHub Container Registry:
#   KAMAL_REGISTRY_PASSWORD=${GITHUB_TOKEN:?GITHUB_TOKEN must be set}
# For DigitalOcean:
#   KAMAL_REGISTRY_PASSWORD=${DO_REGISTRY_TOKEN:?DO_REGISTRY_TOKEN must be set}

# 4. Database Password (if using external PostgreSQL)
# TODO: Uncomment if using database password authentication
# GOLFCOACHAPP_DATABASE_PASSWORD=${DB_PASSWORD:?DB_PASSWORD must be set}

# 5. PostgreSQL Password (if using Kamal database accessory)
# TODO: Uncomment if running PostgreSQL via Kamal accessories
# POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?POSTGRES_PASSWORD must be set}

# ===== EXAMPLES FROM OTHER PASSWORD MANAGERS =====
# Example of extracting secrets from 1password (or another compatible pw manager)
# SECRETS=$(kamal secrets fetch --adapter 1password --account your-account --from Vault/Item KAMAL_REGISTRY_PASSWORD RAILS_MASTER_KEY CLAUDE_API_KEY)
# KAMAL_REGISTRY_PASSWORD=$(kamal secrets extract KAMAL_REGISTRY_PASSWORD ${SECRETS})
# RAILS_MASTER_KEY=$(kamal secrets extract RAILS_MASTER_KEY ${SECRETS})
# CLAUDE_API_KEY=$(kamal secrets extract CLAUDE_API_KEY ${SECRETS})
