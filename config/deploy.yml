# Name of your application. Used to uniquely configure containers.
service: golfcoachapp

# Name of the container image (use your-user/app-name on external registries).
# TODO: Update this to match your registry username/image name
# Examples:
#   - Docker Hub: yourusername/golfcoachapp
#   - GitHub: ghcr.io/yourusername/golfcoachapp
image: golfcoachapp

# Deploy to these servers.
# TODO: Replace 192.168.0.1 with your actual production server IP address
servers:
  web:
    - 192.168.0.1  # CHANGE THIS to your server IP
  # job:
  #   hosts:
  #     - 192.168.0.1
  #   cmd: bin/jobs

# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# If used with Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
#
# Using an SSL proxy like this requires turning on config.assume_ssl and config.force_ssl in production.rb!
#
# Don't use this when deploying to multiple web servers (then you have to terminate SSL at your load balancer).
#
# proxy:
#   ssl: true
#   host: app.example.com

# Where you keep your container images.
# TODO: Choose and configure your container registry
registry:
  # Option 1: Docker Hub (most common)
  #   server: hub.docker.com
  #   username: yourdockerhubusername
  #   password:
  #     - KAMAL_REGISTRY_PASSWORD

  # Option 2: GitHub Container Registry (free for public repos)
  #   server: ghcr.io
  #   username: yourgithubusername
  #   password:
  #     - KAMAL_REGISTRY_PASSWORD  # Use GitHub Personal Access Token

  # Option 3: DigitalOcean Container Registry
  #   server: registry.digitalocean.com/your-registry-name
  #   username: your-do-token
  #   password:
  #     - KAMAL_REGISTRY_PASSWORD

  # TEMPORARY: localhost registry (REMOVE BEFORE DEPLOYING)
  server: localhost:5555

# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - CLAUDE_API_KEY  # Required for AI training plan generation
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    SOLID_QUEUE_IN_PUMA: true

    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # TODO: Configure your PostgreSQL database connection
    # Option 1: External managed database (recommended for production)
    #   DB_HOST: your-postgres-server.com
    #   DATABASE_URL: postgresql://user:password@host:5432/golfcoachapp_production
    # Option 2: Use Kamal database accessory (see accessories section below)
    #   DB_HOST: golfcoachapp-db
    # IMPORTANT: Your app uses 4 databases (primary, cache, queue, cable)
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole --include-password"

# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "golfcoachapp_storage:/rails/storage"

# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets


# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: ruby-3.4.7
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

# Use a different ssh user than root
# ssh:
#   user: app

# Use accessory services (secrets come from .kamal/secrets).
# TODO: Uncomment and configure if you want to run PostgreSQL on the same server
# IMPORTANT: Your app needs 4 PostgreSQL databases:
#   - golfcoachapp_production (primary data)
#   - golfcoachapp_production_cache (Solid Cache)
#   - golfcoachapp_production_queue (Solid Queue)
#   - golfcoachapp_production_cable (Action Cable)
#
# Option 1: Use external managed database service (RECOMMENDED)
#   - Set DB_HOST and DATABASE_URL in env.clear above
#   - Or use GOLFCOACHAPP_DATABASE_PASSWORD in config/database.yml
#
# Option 2: Use Kamal accessory (example below - needs customization)
# accessories:
#   db:
#     image: postgres:16
#     host: 192.168.0.1  # Same as your web server
#     port: "127.0.0.1:5432:5432"
#     env:
#       clear:
#         POSTGRES_USER: golfcoachapp
#         POSTGRES_DB: golfcoachapp_production
#       secret:
#         - POSTGRES_PASSWORD
#     directories:
#       - data:/var/lib/postgresql/data
#   redis:
#     image: valkey/valkey:8
#     host: 192.168.0.2
#     port: 6379
#     directories:
#       - data:/data
